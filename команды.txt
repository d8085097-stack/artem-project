npm run dev          # запускает всё сразу (сервер + витрина + лендинг)
npm run server       # только бэкенд :3001
npm run store        # только витрина :5173
npm run landing      # только лендинг :3000
npm run install:all  # установить зависимости во всех папках